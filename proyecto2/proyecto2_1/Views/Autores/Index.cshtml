@model IEnumerable<proyecto2_1.Models.Autores>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Autores</h2>

<p>
    @Html.ActionLink("Agregar Autor", "Create")
</p>
<div class="jumbotron">
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Autor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Apellido)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Autor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Apellido)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Id_Autor }) |
            @Html.ActionLink("Mostrar", "Details", new { id=item.Id_Autor }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.Id_Autor })
        </td>
    </tr>
}

</table>
    </div>