@model IEnumerable<proyecto2_1.Models.Idioma>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Idiomas</h2>

<p>
    @Html.ActionLink("Agregar Idioma", "Create")
</p>
<div class="jumbotron">
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Idioma1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Idioma1)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Id_Idioma }) |
            @Html.ActionLink("Mostrar", "Details", new { id=item.Id_Idioma }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.Id_Idioma })
        </td>
    </tr>
}

</table>
</div>